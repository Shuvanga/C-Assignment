#include <stdio.h>

   int main() {
     double result; /* The result of our calculation */
     result = (3.0 + 1.0) / 5.0; /* Converting the integer to double as the result is in double */
     printf("The value of 4/5 is %lf\n", result);/* Printing the result */
     return 0;
   }
